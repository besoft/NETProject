<UserControl x:Class="Zcu.StudentEvaluator.View.StudentListViewItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vmd="clr-namespace:Zcu.StudentEvaluator.DesignData;assembly=StudentEvaluatorCoreDesignData"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <vmd:DesignDataContext x:Key="DesignDataContext"/>
    </UserControl.Resources>
    <Grid d:DataContext="{Binding Students.Items[0], Source={StaticResource DesignDataContext}}" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock TextWrapping="Wrap" Text="{Binding FullName, Mode=OneWay}"/>
        <TextBlock TextWrapping="Wrap" Text="{Binding PersonalNumber, Mode=OneWay}" Grid.Column="1"/>
        <TextBlock TextWrapping="Wrap" Grid.Column="2">
            evaluations: <Run Text="{Binding Evaluations.Count, Mode=OneWay}"/>
        </TextBlock>
    </Grid>
</UserControl>
