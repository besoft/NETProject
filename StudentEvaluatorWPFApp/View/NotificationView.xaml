<UserControl x:Name="userControl" x:Class="Zcu.StudentEvaluator.View.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="600"             
             >
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Foreground">
                <Setter.Value>
                    <SolidColorBrush Color="{Binding TextColor, ElementName=userControl, Mode=OneWay}"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Expander x:Name="Expander" Margin="5,5" Header="Info" HorizontalAlignment="Stretch" VerticalAlignment="Top" IsExpanded="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding Title, ElementName=userControl}" Grid.Row="0" FontWeight="Bold"/>
            <TextBlock Text="{Binding Text, ElementName=userControl}" Grid.Row="1"/>    
        </Grid>
    </Expander>    
</UserControl>
